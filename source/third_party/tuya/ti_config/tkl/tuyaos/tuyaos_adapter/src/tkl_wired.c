/**
 * @file tkl_wired.c
 * @brief this file was auto-generated by tuyaos v&v tools, developer can add implements between BEGIN and END
 *
 * @warning: changes between user 'BEGIN' and 'END' will be keeped when run tuyaos v&v tools
 *           changes in other place will be overwrited and lost
 *
 * @copyright Copyright 2020-2021 Tuya Inc. All Rights Reserved.
 *
 */

// --- BEGIN: user defines and implements ---
#include "tkl_wired.h"
#include "tuya_error_code.h"

#include <stdio.h>
#include <string.h>

/* =========================
 * PJ-style feature toggles
 * Default to 0 if not provided by build system.
 * ========================= */
#ifndef PJ_HAS_SYS_TYPES_H
#define PJ_HAS_SYS_TYPES_H 0
#endif
#ifndef PJ_HAS_SYS_SOCKET_H
#define PJ_HAS_SYS_SOCKET_H 0
#endif
#ifndef PJ_HAS_SYS_IOCTL_H
#define PJ_HAS_SYS_IOCTL_H 0
#endif
#ifndef PJ_HAS_NET_IF_H
#define PJ_HAS_NET_IF_H 0
#endif
#ifndef PJ_HAS_ARPA_INET_H
#define PJ_HAS_ARPA_INET_H 0
#endif
#ifndef PJ_HAS_UNISTD_H
#define PJ_HAS_UNISTD_H 0
#endif
#ifndef PJ_HAS_PTHREAD_H
#define PJ_HAS_PTHREAD_H 0
#endif
#ifndef PJ_HAS_ERRNO_H
#define PJ_HAS_ERRNO_H 0
#endif

/* POSIX wired requires these headers */
#if (PJ_HAS_SYS_TYPES_H && PJ_HAS_SYS_SOCKET_H && PJ_HAS_SYS_IOCTL_H && PJ_HAS_NET_IF_H && PJ_HAS_ARPA_INET_H && \
     PJ_HAS_UNISTD_H && PJ_HAS_PTHREAD_H && PJ_HAS_ERRNO_H)
#define TKL_WIRED_POSIX 1
#else
#define TKL_WIRED_POSIX 0
#endif

#if defined(PJ_HAS_SYS_TYPES_H) && PJ_HAS_SYS_TYPES_H != 0
#include <sys/types.h>
#endif

#if defined(PJ_HAS_SYS_SOCKET_H) && PJ_HAS_SYS_SOCKET_H != 0
#include <sys/socket.h>
#endif

#if defined(PJ_HAS_SYS_IOCTL_H) && PJ_HAS_SYS_IOCTL_H != 0
#include <sys/ioctl.h> /* FBIONBIO */
#endif

#if defined(PJ_HAS_NET_IF_H) && PJ_HAS_NET_IF_H != 0
#include <net/if.h>
#endif

#if defined(PJ_HAS_ARPA_INET_H) && PJ_HAS_ARPA_INET_H != 0
#include <arpa/inet.h>
#endif

#if defined(PJ_HAS_UNISTD_H) && PJ_HAS_UNISTD_H != 0
#include <unistd.h>
#endif

#if defined(PJ_HAS_PTHREAD_H) && PJ_HAS_PTHREAD_H != 0
#include <pthread.h>
#endif

#if defined(PJ_HAS_ERRNO_H) && PJ_HAS_ERRNO_H != 0
#include <errno.h>
#endif

/* Keep symbol available on all platforms */
TKL_WIRED_STATUS_CHANGE_CB status_cb = NULL;

#if (TKL_WIRED_POSIX != 0)

#define ROUTE_PATH "/proc/net/route"

static char *net_utils_space_trim(char *str)
{
    int count = 0;
    int size = 0;

    char *tmp = NULL;
    if (!str) {
        return NULL;
    }

    size = (int)strlen(str);
    tmp = str;

    while (*str == ' ' || (unsigned char)(*str - 9) <= (13 - 9)) {
        str++;
        count++;
    }

    if (count) {
        memmove(tmp, str, (size_t)(size - count));
        memset(tmp + (size - count), 0x0, (size_t)count);
    }

    return (char *)str;
}

// only find first devname
static int net_utils_get_devname(char *devname, int size)
{
    FILE *fp = NULL;
    char *fpath = "/proc/net/dev";
    char buf[512];
    int ret = -1;
    char ifname[IFNAMSIZ];
    char *tmp = NULL;

    if (!devname || size <= 0) {
        printf("%s %d: arg err\n", __func__, __LINE__);
        return -1;
    }

    fp = fopen(fpath, "r");
    if (!fp) {
        printf("%s %d: %s is not exist\n", __func__, __LINE__, fpath);
        return -1;
    }

    memset(devname, 0x0, (size_t)size);
    (void)fgets(buf, sizeof(buf), fp);
    (void)fgets(buf, sizeof(buf), fp);

    while (fgets(buf, sizeof(buf), fp)) {
        (void)sscanf(buf, "%[^:]:", ifname);

        tmp = net_utils_space_trim(ifname);
        if (!tmp) {
            printf("%s %d: %s error\n", __func__, __LINE__, ifname);
            continue;
        } else {
            if (strncmp(ifname, "en", 2) == 0 || strncmp(ifname, "eth", 3) == 0) {
                memcpy(devname, ifname, (size_t)size);
                ret = 0;
                break;
            }
        }
    }

    if (fp) {
        fclose(fp);
    }

    return ret;
}

static int net_utils_check_cable(const char *ifname, int *plugged)
{
    int sk = -1;
    struct ifreq ifr;
    int ret = -1;

    if (!ifname) {
        printf("%s %d: ifname is null\n", __func__, __LINE__);
        return -1;
    }

    sk = socket(AF_INET, SOCK_DGRAM, 0);
    if (sk < 0) {
        printf("%s %d: failed to create sk\n", __func__, __LINE__);
        return -1;
    }

    memset(&ifr, 0, sizeof(ifr));
    strncpy(ifr.ifr_name, ifname, sizeof(ifr.ifr_name) - 1);

    ret = ioctl(sk, SIOCGIFFLAGS, &ifr);
    if (ret < 0) {
        printf("%s %d: failed to get iterface info \n", __func__, __LINE__);
        goto exit_end;
    }

    if (ifr.ifr_flags & IFF_RUNNING) {
        *plugged = 1;
    } else {
        *plugged = 0;
    }

exit_end:
    if (sk >= 0) {
        close(sk);
    }
    return ret;
}

static void *link_status_thread(void *arg)
{
    (void)arg;

    TKL_WIRED_STAT_E status;
    int old_status = -1;

    while (1) {
        tkl_wired_get_status(&status);
        if ((int)status == old_status) {
            sleep(1);
            continue;
        }
        old_status = (int)status;

        if (status_cb) {
            status_cb(status);
        }
        sleep(1);
    }
}

static OPERATE_RET tkl_wired_get_gateway(NW_IP_S *ip)
{
    FILE *fp = NULL;
    char buf[256] = {0};
    unsigned int dest = 0;
    struct in_addr addr;
    int get_gw = 0;

    if ((fp = fopen(ROUTE_PATH, "r")) == NULL) {
        printf("open %s failed, %d, %s\n", ROUTE_PATH, errno, strerror(errno));
        return OPRT_COM_ERROR;
    }

    (void)fgets(buf, sizeof(buf), fp);

    while (fgets(buf, sizeof(buf), fp)) {
        (void)sscanf(buf, "%*s%x %x", &dest, &addr.s_addr);
        if ((dest == 0) && (addr.s_addr != 0)) {
            strncpy(ip->gw, inet_ntoa(addr), sizeof(ip->gw));
            get_gw = 1;
            break;
        }
    }

    if (get_gw == 0)
        printf("not find gw addr\n");

    fclose(fp);
    return OPRT_OK;
}

#endif /* TKL_WIRED_POSIX */

// --- END: user defines and implements ---


/**
 * @brief  get the link status of wired link
 */
OPERATE_RET tkl_wired_get_status(TKL_WIRED_STAT_E *status)
{
    // --- BEGIN: user implements ---
#if (TKL_WIRED_POSIX != 0)
    int sock = -1;
    struct ifreq ifr;
    char devname[IFNAMSIZ];
    int ret = -1;
    int plugged = 0;

    if (status == NULL) {
        return OPRT_INVALID_PARM;
    }

    ret = net_utils_get_devname(devname, sizeof(devname));
    if (ret < 0) {
        printf("%s %d: not get devname\n", __func__, __LINE__);
        return OPRT_COM_ERROR;
    }

    if ((sock = socket(AF_INET, SOCK_STREAM, 0)) < 0) {
        printf("socket create fail\n");
        return OPRT_SOCK_ERR;
    }

    ret = net_utils_check_cable(devname, &plugged);
    if (!plugged) {
        close(sock);
        *status = TKL_WIRED_LINK_DOWN;
        return OPRT_OK;
    }

    memset(&ifr, 0x0, sizeof(ifr));
    strncpy(ifr.ifr_name, devname, sizeof(ifr.ifr_name) - 1);

    if (ioctl(sock, SIOCGIFADDR, &ifr) < 0) {
        printf("%s %d: %s get ip, error=%d,%s\n", __func__, __LINE__, devname, errno, strerror(errno));
        close(sock);
        *status = TKL_WIRED_LINK_DOWN;
        return OPRT_OK;
    }

    close(sock);
    *status = TKL_WIRED_LINK_UP;
    return OPRT_OK;
#else
    if (status) {
        *status = TKL_WIRED_LINK_DOWN;
    }
    return OPRT_NOT_SUPPORTED;
#endif
    // --- END: user implements ---
}


/**
 * @brief  set the status change callback
 */
OPERATE_RET tkl_wired_set_status_cb(TKL_WIRED_STATUS_CHANGE_CB cb)
{
    // --- BEGIN: user implements ---
#if (TKL_WIRED_POSIX != 0)
    pthread_t thread;
    status_cb = cb;
    return pthread_create(&thread, NULL, link_status_thread, NULL);
#else
    status_cb = cb;
    return OPRT_NOT_SUPPORTED;
#endif
    // --- END: user implements ---
}


/**
 * @brief  get the ip address of the wired link
 */
OPERATE_RET tkl_wired_get_ip(NW_IP_S *ip)
{
    // --- BEGIN: user implements ---
#if (TKL_WIRED_POSIX != 0)
    int sock = -1;
    struct ifreq ifr;
    struct sockaddr_in *sin;
    char devname[IFNAMSIZ];
    int ret = -1;

    if (ip == NULL) {
        printf("invalid param\n");
        return OPRT_INVALID_PARM;
    }

    ret = net_utils_get_devname(devname, sizeof(devname));
    if (ret < 0) {
        printf("%s %d: not get devname\n", __func__, __LINE__);
        return OPRT_COM_ERROR;
    }

    printf("%s %d: ifname=%s\n", __func__, __LINE__, devname);

    if ((sock = socket(AF_INET, SOCK_STREAM, 0)) < 0) {
        printf("socket create failed\n");
        return OPRT_SOCK_ERR;
    }

    memset(ip, 0x0, sizeof(NW_IP_S));
    memset(&ifr, 0x0, sizeof(ifr));
    strncpy(ifr.ifr_name, devname, sizeof(ifr.ifr_name) - 1);
    sin = (struct sockaddr_in *)&(ifr.ifr_addr);

    if (ioctl(sock, SIOCGIFADDR, &ifr) < 0) {
        printf("%s %d: ip ioctl %s, error=%d,%s\n", __func__, __LINE__, devname, errno, strerror(errno));
        goto com_error;
    }
    strncpy(ip->ip, inet_ntoa(sin->sin_addr), sizeof(ip->ip));

    if (ioctl(sock, SIOCGIFNETMASK, &ifr) < 0) {
        printf("%s %d: mask ioctl %s, error=%d,%s\n", __func__, __LINE__, devname, errno, strerror(errno));
        goto com_error;
    }
    strncpy(ip->mask, inet_ntoa(sin->sin_addr), sizeof(ip->mask));

    if (tkl_wired_get_gateway(ip) < 0)
        goto com_error;

    close(sock);
    return OPRT_OK;

com_error:
    close(sock);
    return OPRT_COM_ERROR;
#else
    if (ip == NULL) {
        return OPRT_INVALID_PARM;
    }
    memset(ip, 0, sizeof(NW_IP_S));
    return OPRT_NOT_SUPPORTED;
#endif
    // --- END: user implements ---
}


/**
 * @brief  get the mac address of the wired link
 */
OPERATE_RET tkl_wired_get_mac(NW_MAC_S *mac)
{
    // --- BEGIN: user implements ---
#if (TKL_WIRED_POSIX != 0)
    int sock = -1;
    struct ifreq ifr;
    struct sockaddr *addr;
    char devname[IFNAMSIZ];
    int ret = -1;

    if (mac == NULL) {
        printf("invalid param\n");
        return OPRT_INVALID_PARM;
    }

    ret = net_utils_get_devname(devname, sizeof(devname));
    if (ret < 0) {
        printf("%s %d: not get devname\n", __func__, __LINE__);
        return OPRT_COM_ERROR;
    }

    printf("%s %d: ifname=%s\n", __func__, __LINE__, devname);

    if ((sock = socket(AF_INET, SOCK_STREAM, 0)) < 0) {
        printf("socket create fail\n");
        return OPRT_SOCK_ERR;
    }

    memset(&ifr, 0x0, sizeof(ifr));
    strncpy(ifr.ifr_name, devname, sizeof(ifr.ifr_name) - 1);
    addr = (struct sockaddr *)&ifr.ifr_hwaddr;
    addr->sa_family = 1;

    if (ioctl(sock, SIOCGIFHWADDR, &ifr) < 0) {
        printf("%s %d: %s get mac, error=%d,%s\n", __func__, __LINE__, devname, errno, strerror(errno));
        close(sock);
        return OPRT_COM_ERROR;
    }

    memcpy(mac->mac, addr->sa_data, MAC_ADDR_LEN);

    close(sock);
    return OPRT_OK;
#else
    if (mac == NULL) {
        return OPRT_INVALID_PARM;
    }
    memset(mac, 0, sizeof(NW_MAC_S));
    return OPRT_NOT_SUPPORTED;
#endif
    // --- END: user implements ---
}


/**
 * @brief  set the mac address of the wired link
 */
OPERATE_RET tkl_wired_set_mac(const NW_MAC_S *mac)
{
    // --- BEGIN: user implements ---
#if (TKL_WIRED_POSIX != 0)
    int sock = -1;
    struct ifreq ifr;
    struct sockaddr *addr;
    char devname[IFNAMSIZ];
    int ret = -1;

    if (mac == NULL) {
        printf("invalid param\n");
        return OPRT_INVALID_PARM;
    }

    printf("%s %d: %02x:%02x:%02x:%02x:%02x:%02x\n", __func__, __LINE__,
           mac->mac[0], mac->mac[1], mac->mac[2], mac->mac[3], mac->mac[4], mac->mac[5]);

    ret = net_utils_get_devname(devname, sizeof(devname));
    if (ret < 0) {
        printf("%s %d: not get devname\n", __func__, __LINE__);
        return OPRT_COM_ERROR;
    }

    printf("%s %d: ifname=%s\n", __func__, __LINE__, devname);

    if ((sock = socket(AF_INET, SOCK_STREAM, 0)) < 0) {
        printf("socket create fail\n");
        return OPRT_SOCK_ERR;
    }

    memset(&ifr, 0x0, sizeof(ifr));
    strncpy(ifr.ifr_name, devname, sizeof(ifr.ifr_name) - 1);
    addr = (struct sockaddr *)&ifr.ifr_hwaddr;
    addr->sa_family = 1;
    memcpy(addr->sa_data, mac->mac, MAC_ADDR_LEN);

    if (ioctl(sock, SIOCSIFHWADDR, &ifr) < 0) {
        printf("%s %d: %s set mac, error=%d,%s\n", __func__, __LINE__, devname, errno, strerror(errno));
        close(sock);
        return OPRT_COM_ERROR;
    }

    close(sock);
    return OPRT_OK;
#else
    if (mac == NULL) {
        return OPRT_INVALID_PARM;
    }
    return OPRT_NOT_SUPPORTED;
#endif
    // --- END: user implements ---
}
